import type { Config } from "tailwindcss";

/** Tailwind v4 config stub for tooling (shadcn / IDE). Theme tokens live in `pages/tailwind.css`. */
export default {
  content: ["./pages/**/*.{ts,tsx}", "./components/**/*.{ts,tsx}"],
} satisfies Config;
